/**
 * Definition of the data structure that is going to be returned by the post
 * If it is an array, define the array element object only.
 */
export interface tPostData {
    userId: number,
    id:     number,
    title:  string,
    body:   string
}
